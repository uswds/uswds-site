name: Hero
contents:
  - name: Hero
    section: true
    subsection: Hero image
    usage: Background image for the hero. Use a path relative to the final project CSS.
    var: $theme-hero-image
    default: "'#{$theme-image-path}/hero.png'"
    kind: path
