name: Accordion
contents:
  - name: Background color
    description: Background color of the accordion content.
    var: $theme-accordion-background-color
    default: "default"
    type: color
  - name: Border width
    description: Border width of the bordered accordion.
    var: $theme-accordion-border-width
    default: 0.5
    type: units
  - name: Border color
    description: Border color of the bordered accordion.
    var: $theme-accordion-border-color
    default: '"base-lightest"'
    type: color
  - name: Button background color
    description: Background color of the accordion button.
    var: $theme-accordion-button-background-color
    default: '"base-lightest"'
    type: color
  - name: Font family
    description: Font family of the accordion.
    var: $theme-accordion-font-family
    default: '"body"'
    type: family
  - name: Icon position
    description: Logical side of the expand/collapse icon on accordion buttons. Allowed values are `"start"` (default) or `"end"`. The default changed from `"end"` in earlier versions. Per-accordion modifiers (`usa-accordion--icon-start`, `usa-accordion--icon-end`) override this setting.
    var: $theme-accordion-icon-position
    default: '"start"'
    type: string
